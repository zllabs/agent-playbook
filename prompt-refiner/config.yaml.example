# Prompt Refiner configuration
# Copy to your project root as prompt-refiner.yaml
#
# mode: auto | review (default: auto)
mode: auto

# Optional: expose generated briefs in auto mode (default: false)
# debug: false
